# See README for how to configure these params

log_level: info
lang: uk
slack_bot_token: <SLACK_BOT_TOKEN>
slack_app_token: <SLACK_APP_TOKEN>
admins:
  - bot_admin_slack_name_1
  - bot_admin_slack_name_2

db: postgresql://admin:qwerty@127.0.0.1:5432/mydb

digest:
  channel: general
  day: 1

karma:
  initial_value: 0
  max_diff: 5
  self_karma: yes
  vote_timeout: 3H
  keep_history: P90DT
  upvote_emoji:
    - "+1"
    - "partyparrot"
    - "thumbsup"
    - "thumbsup_all"
  downvote_emoji:
    - "-1"
    - "thumbsdown"

# See README for how to configure these params
